# Copy to .env.local and fill in. Never commit real values.

# Optional: enable Gmail sync. Create an OAuth 2.0 Client ID (type Web app)
# in the GCP console, add your deployed origin (e.g. https://your-app.web.app)
# and http://localhost:5173 to Authorized JavaScript origins, then paste it
# here. Keep the OAuth consent screen in "External + Testing" and add yourself
# as a test user.
VITE_GOOGLE_CLIENT_ID=
